import React from "react";
import { BrowserRouter, Route, Routes, Navigate } from "react-router-dom";

export default (
  <BrowserRouter>
    <Routes>
      <Route path="/passwd" />
    </Routes>
  </BrowserRouter>
);
